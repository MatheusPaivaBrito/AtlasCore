# notification_api

Platform API for notification intent, templates, channels, delivery attempts, Slack and e-mail providers.

This service is part of the platform profile. It is grouped by capability, not by vendor/tool.

Planned modules:

- `notifications`
- `templates`
- `channels`
- `delivery_attempts`

Provider adapters:

- `slack`
- `sendgrid`
