# <api-a>-<api-b> Contracts

This folder documents contracts between `<api-a>` and `<api-b>`.

## Relationship

Describe why these APIs communicate directly and why the interaction does not belong to another API.

## Active Contracts

| Contract | Version | Direction | Status |
| --- | --- | --- | --- |
| `<capability>` | `v1` | `<api-a> -> <api-b>` | Active |

## Ownership

Describe which API owns the data, which API owns the behavior and which API is only a consumer.

## Evolution Rules

Describe how this relationship can change without breaking existing consumers.
